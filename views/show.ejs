<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= data.title %> | NASA APOD</title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/style.css">
  
  <style>
    body {
      background-color: #0D0D0D;
      color: #EAEAEA;
    }
    .apod-container {
      margin-top: 60px;
      background-color: #1A1A1A;
      border-radius: 15px;  
      padding: 30px;
      box-shadow: 0 0 15px rgba(0, 195, 255, 0.2);
    }
    .apod-title {
      font-size: 2rem;
      margin-bottom: 20px;
    }
    .apod-image {
      border-radius: 10px;
      margin-bottom: 20px;
      max-height: 500px;
      object-fit: contain;
    }
    .back-btn {
      margin-top: 30px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-8 apod-container text-center">
        
        <h1 class="apod-title"><%= data.title %></h1>
        
        <img src="<%= data.url %>" alt="No image found" class="img-fluid apod-image">

        <p class="text-start mt-4"><%= data.explanation %></p>
        
        <form action="/" method="get" class="back-btn">
          <button type="submit" class="btn btn-outline-light">← Back</button>
        </form>

      </div>
    </div>
  </div>
</body>
</html>
